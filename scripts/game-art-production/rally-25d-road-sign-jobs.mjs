#!/usr/bin/env node
import { createHash } from "node:crypto";
import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import process from "node:process";
import { fileURLToPath } from "node:url";

export const SPEC_SCHEMA = "evavo.rally-road-sign-visual-development.v1";
export const SESSION_SCHEMA = "evavo.rally-road-sign-provider-job-session.v1";
export const JOB_SCHEMA = "evavo.rally-road-sign-provider-job.v1";
export const PROTOCOL_VERSION = "2026-08-15.4";
const VARIANTS = new Set(["chevron", "hazard-diamond", "route-arrow", "shared-construction"]);
const ID = /^[a-z0-9]+(?:[a-z0-9-]*[a-z0-9])?$/u;
const SHA = /^[0-9a-f]{64}$/u;
const SPEC_KEYS = new Set(["schema", "protocolVersion", "assetId", "subjectId", "title", "variants", "style", "cameraLocks", "jobs", "output", "authority"]);
const STYLE_KEYS = new Set(["projectionFamily", "designEra", "rendering", "propLanguage", "palette", "surfaceLanguage", "forbidden"]);
const CAMERA_KEYS = new Set(["heroYawDegrees", "heroPitchDegrees", "orthographicPitchDegrees", "focalLengthMm", "consistentPostHeight", "consistentPlateThickness", "consistentFastenerSpacing"]);
const JOB_KEYS = new Set(["id", "role", "variant", "view", "phase", "width", "height", "transparent", "dependsOn", "include", "exclude"]);
const OUTPUT_KEYS = new Set(["workingRoot", "masterRoot", "format", "oneImagePerJob", "retainIndividualSources", "automaticAssembly"]);
const AUTHORITY_FALSE = ["providerExecution", "automaticGenerationAuthorization", "automaticCreativeApproval", "imageMutation", "automaticAssembly", "targetRepositoryMutation", "gitMutation", "deployment", "publication"];
function fail(message){throw new Error(`RALLY_ROAD_SIGN_ART_JOBS_INVALID: ${message}`);}
function object(value,label){if(!value||typeof value!=="object"||Array.isArray(value))fail(`${label} must be an object.`);return value;}
function exact(value,keys,label){object(value,label);const actual=Object.keys(value);const unknown=actual.filter((key)=>!keys.has(key)).sort();const missing=[...keys].filter((key)=>!Object.hasOwn(value,key)).sort();if(unknown.length)fail(`${label} contains unsupported keys: ${unknown.join(", ")}.`);if(missing.length)fail(`${label} is missing keys: ${missing.join(", ")}.`);}
function text(value,label,min=1,max=8000){if(typeof value!=="string"||value.trim()!==value||value.length<min||value.length>max)fail(`${label} must be a trimmed string with ${min}-${max} characters.`);if([...value].some((character)=>character.charCodeAt(0)<32))fail(`${label} contains control characters.`);return value;}
function id(value,label){const result=text(value,label,1,180);if(!ID.test(result))fail(`${label} must be lowercase kebab-case.`);return result;}
function number(value,label,min,max){if(typeof value!=="number"||!Number.isFinite(value)||value<min||value>max)fail(`${label} must be a finite number between ${min} and ${max}.`);return value;}
function strings(value,label,min=0,max=64){if(!Array.isArray(value)||value.length<min||value.length>max)fail(`${label} must contain ${min}-${max} strings.`);const result=value.map((entry,index)=>text(entry,`${label}[${index}]`,1,1000));if(new Set(result).size!==result.length)fail(`${label} contains duplicates.`);return result;}
function safeRoot(value,label){const root=text(value,label,3,500);if(root.startsWith("/")||root.includes("\\")||root.split("/").some((part)=>["",".",".."].includes(part)))fail(`${label} must be a safe relative POSIX path.`);return root;}
function sortValue(value){if(Array.isArray(value))return value.map(sortValue);if(!value||typeof value!=="object")return value;return Object.fromEntries(Object.keys(value).sort().map((key)=>[key,sortValue(value[key])]));}
export function canonicalJson(value){return `${JSON.stringify(sortValue(value))}\n`;}
export function sha256(value){return createHash("sha256").update(canonicalJson(value)).digest("hex");}
function validateAuthority(value){exact(value,new Set([...AUTHORITY_FALSE,"namedHumanApprovalRequired"]),"authority");for(const key of AUTHORITY_FALSE)if(value[key]!==false)fail(`authority.${key} must remain false.`);if(value.namedHumanApprovalRequired!==true)fail("authority.namedHumanApprovalRequired must remain true.");}
function topo(jobs){const byId=new Map(jobs.map((job,index)=>[job.id,{job,index}]));const degree=new Map(jobs.map((job)=>[job.id,job.dependsOn.length]));const consumers=new Map(jobs.map((job)=>[job.id,[]]));for(const job of jobs){for(const dependency of job.dependsOn){if(!byId.has(dependency))fail(`job ${job.id} references unknown dependency ${dependency}.`);if(dependency===job.id)fail(`job ${job.id} depends on itself.`);consumers.get(dependency).push(job.id);}}const ready=jobs.filter((job)=>degree.get(job.id)===0).map((job)=>job.id);ready.sort((a,b)=>byId.get(a).index-byId.get(b).index||a.localeCompare(b));const ordered=[];while(ready.length){const current=ready.shift();ordered.push(byId.get(current).job);for(const consumer of consumers.get(current)){degree.set(consumer,degree.get(consumer)-1);if(degree.get(consumer)===0){ready.push(consumer);ready.sort((a,b)=>byId.get(a).index-byId.get(b).index||a.localeCompare(b));}}}if(ordered.length!==jobs.length)fail("job dependency graph contains a cycle.");return ordered;}
export function validateRoadSignSpec(input){const source=structuredClone(input);exact(source,SPEC_KEYS,"spec");if(source.schema!==SPEC_SCHEMA||source.protocolVersion!==PROTOCOL_VERSION)fail("spec identity drifted.");id(source.assetId,"assetId");id(source.subjectId,"subjectId");text(source.title,"title",3,300);const variants=strings(source.variants,"variants",3,3);if(new Set(variants).size!==3||variants.some((variant)=>!VARIANTS.has(variant)))fail("variant closure drifted.");exact(source.style,STYLE_KEYS,"style");id(source.style.projectionFamily,"style.projectionFamily");id(source.style.designEra,"style.designEra");id(source.style.rendering,"style.rendering");text(source.style.propLanguage,"style.propLanguage",40,1400);strings(source.style.palette,"style.palette",6,16).forEach((colour,index)=>{if(!/^#[0-9a-fA-F]{6}$/u.test(colour))fail(`style.palette[${index}] is invalid.`);});strings(source.style.surfaceLanguage,"style.surfaceLanguage",4,16);strings(source.style.forbidden,"style.forbidden",7,32);exact(source.cameraLocks,CAMERA_KEYS,"cameraLocks");number(source.cameraLocks.heroYawDegrees,"cameraLocks.heroYawDegrees",-180,180);number(source.cameraLocks.heroPitchDegrees,"cameraLocks.heroPitchDegrees",0,89);number(source.cameraLocks.orthographicPitchDegrees,"cameraLocks.orthographicPitchDegrees",-5,5);number(source.cameraLocks.focalLengthMm,"cameraLocks.focalLengthMm",35,200);for(const key of ["consistentPostHeight","consistentPlateThickness","consistentFastenerSpacing"])if(source.cameraLocks[key]!==true)fail(`cameraLocks.${key} must remain true.`);exact(source.output,OUTPUT_KEYS,"output");safeRoot(source.output.workingRoot,"output.workingRoot");safeRoot(source.output.masterRoot,"output.masterRoot");if(source.output.format!=="png"||source.output.oneImagePerJob!==true||source.output.retainIndividualSources!==true||source.output.automaticAssembly!==false)fail("output contract drifted.");validateAuthority(source.authority);if(!Array.isArray(source.jobs)||source.jobs.length!==10)fail("jobs must contain exactly ten one-image jobs.");const ids=new Set();const jobs=source.jobs.map((raw,index)=>{exact(raw,JOB_KEYS,`jobs[${index}]`);const jobId=id(raw.id,`jobs[${index}].id`);if(ids.has(jobId))fail(`duplicate job id ${jobId}.`);ids.add(jobId);const role=id(raw.role,`jobs[${index}].role`);const variant=id(raw.variant,`jobs[${index}].variant`);if(!VARIANTS.has(variant))fail(`jobs[${index}].variant is unsupported.`);const view=id(raw.view,`jobs[${index}].view`);const phase=id(raw.phase,`jobs[${index}].phase`);for(const dimension of ["width","height"])if(!Number.isInteger(raw[dimension])||raw[dimension]<512||raw[dimension]>4096)fail(`jobs[${index}].${dimension} is invalid.`);if(typeof raw.transparent!=="boolean")fail(`jobs[${index}].transparent must be boolean.`);const dependsOn=strings(raw.dependsOn,`jobs[${index}].dependsOn`).map((entry)=>id(entry,`jobs[${index}].dependsOn`));const include=strings(raw.include,`jobs[${index}].include`,2,24);const exclude=strings(raw.exclude,`jobs[${index}].exclude`,2,24);if(include.some((entry)=>/contact\s*sheet|multi[- ]panel|grid of|multiple unrelated/i.test(entry)))fail(`jobs[${index}] requests a forbidden combined layout.`);return{...raw,id:jobId,role,variant,view,phase,dependsOn,include,exclude};});topo(jobs);if(jobs.filter((job)=>job.role==="modular-modeling-reference").length!==4)fail("road-sign kit requires four modular modeling references.");if(jobs.filter((job)=>job.role==="breakable-damage-reference").length!==2)fail("road-sign kit requires two breakable damage references.");for(const variant of source.variants){if(!jobs.some((job)=>job.variant===variant&&["identity-master","variant-identity"].includes(job.role)))fail(`variant identity is missing ${variant}.`);}return source;}
function prompt(spec,job){return[`Create exactly one image for ${spec.title}.`,`Asset identity: ${spec.subjectId}; variant: ${job.variant}; job: ${job.id}; role: ${job.role}; view: ${job.view}.`,`Visual target: ${spec.style.designEra}; ${spec.style.rendering}; ${spec.style.propLanguage}`,`Projection: ${spec.style.projectionFamily}. Preserve post height, plate thickness, fastener spacing, shared material family and the exact symbol identity established by dependencies.`,`Include: ${job.include.join("; ")}.`,`Avoid: ${[...spec.style.forbidden,...job.exclude].join("; ")}.`,`Output one ${job.width}x${job.height} PNG${job.transparent?" with true transparent background":" as one coherent material view"}. No labels, no contact sheet, no grid, no second prop.`].join("\n");}
export function compileRoadSignSession(input){const spec=validateRoadSignSpec(input);const specSha256=sha256(spec);const jobs=topo(spec.jobs).map((job,sequence)=>{const body={schema:JOB_SCHEMA,protocolVersion:PROTOCOL_VERSION,sequence,assetId:spec.assetId,subjectId:spec.subjectId,variantId:job.variant,jobId:job.id,phase:job.phase,role:job.role,view:job.view,operation:"generate",images:1,candidateCount:1,target:{width:job.width,height:job.height,format:"png",transparentBackground:job.transparent},dependencies:[...job.dependsOn],prompt:prompt(spec,job),output:{working:`${spec.output.workingRoot}/${job.id}.png`,master:`${spec.output.masterRoot}/${job.id}.png`},idempotencyKey:sha256({specSha256,jobId:job.id,variant:job.variant,dependencies:job.dependsOn}),authority:{providerExecution:false,automaticApproval:false,imageMutation:false,automaticAssembly:false,repositoryMutation:false,gitMutation:false,deployment:false,publication:false,namedHumanApprovalRequired:true}};return{...body,jobSha256:sha256(body)};});const body={schema:SESSION_SCHEMA,protocolVersion:PROTOCOL_VERSION,sessionId:`${spec.assetId}-visual-development-v1`,assetId:spec.assetId,subjectId:spec.subjectId,specSha256,spec,jobs,totals:{jobs:jobs.length,images:jobs.length,variants:spec.variants.length,modelingJobs:jobs.filter((job)=>job.role==="modular-modeling-reference").length,damageJobs:jobs.filter((job)=>job.role==="breakable-damage-reference").length,transparentJobs:jobs.filter((job)=>job.target.transparentBackground).length},readiness:{status:"provider-jobs-compiled",providerExecutionRequired:true,namedHumanApprovalRequired:true,downstream3DReady:false},authority:{providerExecution:false,automaticGenerationAuthorization:false,automaticCreativeApproval:false,imageMutation:false,automaticAssembly:false,targetRepositoryMutation:false,gitMutation:false,deployment:false,publication:false,namedHumanApprovalRequired:true}};return{...body,sessionSha256:sha256(body)};}
export function verifyRoadSignSession(session){if(!session||typeof session!=="object"||Array.isArray(session))fail("session must be an object.");if(session.schema!==SESSION_SCHEMA||session.protocolVersion!==PROTOCOL_VERSION)fail("session identity drifted.");if(typeof session.sessionSha256!=="string"||!SHA.test(session.sessionSha256))fail("sessionSha256 is invalid.");const body=Object.fromEntries(Object.entries(session).filter(([key])=>key!=="sessionSha256"));if(sha256(body)!==session.sessionSha256)fail("sessionSha256 does not match the submitted payload.");const expected=compileRoadSignSession(session.spec);if(canonicalJson(expected)!==canonicalJson(session))fail("session is not the deterministic compilation of its retained spec.");return true;}
async function main(){const[command,inputPath,...rest]=process.argv.slice(2);const outputIndex=rest.indexOf("--output");const outputPath=outputIndex>=0?rest[outputIndex+1]:undefined;if(!["compile","verify"].includes(command)||!inputPath)fail("Usage: rally-25d-road-sign-jobs.mjs <compile|verify> <input.json> [--output output.json]");const input=JSON.parse(await readFile(inputPath,"utf8"));const result=command==="compile"?compileRoadSignSession(input):{valid:verifyRoadSignSession(input),sessionId:input.sessionId,sessionSha256:input.sessionSha256,jobs:input.totals.jobs,images:input.totals.images,variants:input.totals.variants,modelingJobs:input.totals.modelingJobs,damageJobs:input.totals.damageJobs,oneImagePerJob:input.totals.jobs===input.totals.images,providerExecution:false};const rendered=`${JSON.stringify(result,null,2)}\n`;if(outputPath)await writeFile(outputPath,rendered,"utf8");process.stdout.write(rendered);}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){main().catch((error)=>{process.stderr.write(`${error instanceof Error?error.message:String(error)}\n`);process.exitCode=1;});}
