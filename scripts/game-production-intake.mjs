export const GAME_PRODUCTION_STUDIO_ID='art-studio';
export const GAME_PRODUCTION_REPOSITORY='EVAVO-STUDIO/evavo-art-studio';
export const GAME_PRODUCTION_SCHEMA_VERSION='1.0.0';
export const GAME_PRODUCTION_RECEIPT_SCHEMA_VERSION='1.0.0';
const PLAN_LIMIT=4*1024*1024, RECEIPT_LIMIT=1024*1024;
function cloneBounded(value,limit=PLAN_LIMIT){let visits=0;const text=JSON.stringify(value,(_,item)=>{if(++visits>300000||item===undefined||['function','symbol','bigint'].includes(typeof item)||(typeof item==='number'&&!Number.isFinite(item)))throw new Error('Expected bounded, dense, finite JSON.');return item;});if(Buffer.byteLength(text,'utf8')>limit)throw new Error('Creative production input exceeds its byte limit.');return JSON.parse(text);}
const required=(value,name)=>{if(typeof value!=='string'||!value)throw new Error(`${name} must be a non-empty string.`);return value;};
function validatePacket(packet){if(!packet||typeof packet!=='object'||Array.isArray(packet))throw new Error('Production packet must be an object.');for(const key of ['id','kind','status','sourceDigest','sourceAssetId','sourceCategory','title','studio','repository','operation','deliverable'])required(packet[key],key);if(packet.kind!=='creative-production-packet')throw new Error(`Packet ${packet.id} has unsupported kind.`);if(packet.status!=='queued-not-executed')throw new Error(`Packet ${packet.id} must remain queued-not-executed at intake.`);if(packet.studio!==GAME_PRODUCTION_STUDIO_ID||packet.repository!==GAME_PRODUCTION_REPOSITORY)throw new Error(`Packet ${packet.id} targets the wrong studio or repository.`);for(const key of ['dependsOn','acceptance','sourceRefs','notes'])if(!Array.isArray(packet[key]))throw new Error(`Packet ${packet.id} has invalid ${key}.`);if(packet.dependsOn.some(dep=>typeof dep!=='string'||!dep))throw new Error(`Packet ${packet.id} has invalid dependencies.`);for(const key of ['execute','approve','mutateConsumerRepository','publish'])if(packet.authority?.[key]!==false)throw new Error(`Packet ${packet.id} attempts to escalate ${key} authority.`);}
function validateReceipt(receipt){if(!receipt||typeof receipt!=='object'||Array.isArray(receipt))throw new Error('Completed receipt must be an object.');if(receipt.kind!=='creative-production-receipt'||receipt.schemaVersion!==GAME_PRODUCTION_RECEIPT_SCHEMA_VERSION||receipt.status!=='completed-reviewed')throw new Error('Completed receipt must be creative-production-receipt v1.0.0 with completed-reviewed status.');for(const key of ['packetId','sourceDigest','studio','repository','operation'])required(receipt[key],`receipt.${key}`);required(receipt.artifactRevision?.id,'receipt.artifactRevision.id');required(receipt.artifactRevision?.digest,'receipt.artifactRevision.digest');if(!Array.isArray(receipt.evidenceRefs)||receipt.evidenceRefs.length===0)throw new Error(`Receipt ${receipt.packetId} requires evidenceRefs.`);receipt.evidenceRefs.forEach((ref,i)=>{required(ref?.kind,`receipt.evidenceRefs[${i}].kind`);required(ref?.ref,`receipt.evidenceRefs[${i}].ref`);if(Object.hasOwn(ref??{},'digest'))required(ref.digest,`receipt.evidenceRefs[${i}].digest`);});if(receipt.review?.status!=='reviewed')throw new Error(`Receipt ${receipt.packetId} must be reviewed.`);required(receipt.review?.reviewer,'receipt.review.reviewer');required(receipt.review?.evidenceDigest,'receipt.review.evidenceDigest');for(const key of ['executesProduction','grantsCreativeApproval','mutatesConsumerRepository','publishes'])if(receipt.authority?.[key]!==false)throw new Error(`Receipt ${receipt.packetId} attempts to escalate ${key} authority.`);}
function bindReceipt(receipt,plan,byId){validateReceipt(receipt);if(receipt.sourceDigest!==plan.source.sourceDigest)throw new Error(`Receipt ${receipt.packetId} sourceDigest does not match the plan.`);const packet=byId.get(receipt.packetId);if(!packet)throw new Error(`Receipt ${receipt.packetId} references an unknown packet.`);for(const [rk,pk] of [['sourceDigest','sourceDigest'],['studio','studio'],['repository','repository'],['operation','operation']])if(receipt[rk]!==packet[pk])throw new Error(`Receipt ${receipt.packetId} ${rk} does not match its source packet.`);return packet;}
export function createGameProductionReceipt({packet,artifactRevision,evidenceRefs,review}){const source=cloneBounded(packet,RECEIPT_LIMIT);validatePacket(source);const receipt={kind:'creative-production-receipt',schemaVersion:GAME_PRODUCTION_RECEIPT_SCHEMA_VERSION,status:'completed-reviewed',packetId:source.id,sourceDigest:source.sourceDigest,studio:source.studio,repository:source.repository,operation:source.operation,artifactRevision:cloneBounded(artifactRevision,RECEIPT_LIMIT),evidenceRefs:cloneBounded(evidenceRefs,RECEIPT_LIMIT),review:cloneBounded(review,RECEIPT_LIMIT),authority:{executesProduction:false,grantsCreativeApproval:false,mutatesConsumerRepository:false,publishes:false}};validateReceipt(receipt);return receipt;}
export function admitGameProductionPlan(input,{approvedGates=[],completedReceipts=[]}={}){const plan=cloneBounded(input);if(plan?.kind!=='creative-production-plan'||plan.schemaVersion!==GAME_PRODUCTION_SCHEMA_VERSION)throw new Error('Expected creative-production-plan schemaVersion 1.0.0.');required(plan?.source?.sourceDigest,'source.sourceDigest');if(!Array.isArray(plan.packets)||!Array.isArray(approvedGates)||!Array.isArray(completedReceipts))throw new Error('packets, approvedGates and completedReceipts must be arrays.');const gates=new Set(approvedGates);if([...gates].some(x=>typeof x!=='string'||!x))throw new Error('approvedGates must contain non-empty strings.');const byId=new Map();for(const packet of plan.packets){required(packet?.id,'packet.id');if(byId.has(packet.id))throw new Error(`Duplicate packet id: ${packet.id}.`);if(packet.sourceDigest!==plan.source.sourceDigest)throw new Error(`Packet ${packet.id} sourceDigest does not match the plan source.`);byId.set(packet.id,packet);}const receipts=new Map();for(const raw of completedReceipts){const receipt=cloneBounded(raw,RECEIPT_LIMIT);bindReceipt(receipt,plan,byId);if(receipts.has(receipt.packetId))throw new Error(`Duplicate completed receipt for ${receipt.packetId}.`);receipts.set(receipt.packetId,receipt);}const selected=plan.packets.filter(packet=>packet.studio===GAME_PRODUCTION_STUDIO_ID||packet.repository===GAME_PRODUCTION_REPOSITORY);selected.forEach(validatePacket);const packets=selected.map(packet=>{const blockers=[],resolvedDependencies=[];for(const dep of packet.dependsOn){if(dep.startsWith('gate/')){if(!gates.has(dep))blockers.push({code:'GATE_REQUIRED',dependency:dep});else resolvedDependencies.push({kind:'gate',id:dep});}else if(!byId.has(dep))blockers.push({code:'UNKNOWN_DEPENDENCY',dependency:dep});else{const receipt=receipts.get(dep);if(!receipt)blockers.push({code:'REVIEWED_RECEIPT_REQUIRED',dependency:dep});else resolvedDependencies.push({kind:'creative-production-receipt',packetId:receipt.packetId,studio:receipt.studio,repository:receipt.repository,operation:receipt.operation,artifactRevision:receipt.artifactRevision,review:receipt.review,evidenceRefs:receipt.evidenceRefs});}}return{...cloneBounded(packet),intake:{readiness:blockers.length?'blocked':'ready-for-studio-execution-request',blockers,resolvedDependencies,executionAuthorized:false,approvalGranted:false}};});const readyCount=packets.filter(x=>x.intake.readiness==='ready-for-studio-execution-request').length;return{kind:'creative-production-studio-intake',schemaVersion:GAME_PRODUCTION_SCHEMA_VERSION,status:packets.length===0?'no-work-for-studio':readyCount===packets.length?'ready-for-explicit-execution-request':'blocked-on-dependencies',sourceDigest:plan.source.sourceDigest,studio:GAME_PRODUCTION_STUDIO_ID,repository:GAME_PRODUCTION_REPOSITORY,packetCount:packets.length,readyCount,receiptCount:receipts.size,packets,authority:{executesProduction:false,approvesCreativeWork:false,mutatesConsumerRepository:false,publishes:false},requirements:['Lock silhouette, palette, material language, camera/readability and target display scale before downstream production.','Use existing Art Studio provider admission, review and workspace authorization after intake; readiness is not execution authorization.']};}
