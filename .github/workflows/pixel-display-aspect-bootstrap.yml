name: Retired Pixel Typography display-aspect bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm retirement
        run: echo "The one-time display-aspect bootstrap has completed and is being removed."
