name: Agent Apply Book Art Legacy Byte Registration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Bootstrap transport is being replaced with checksummed chunks.'
