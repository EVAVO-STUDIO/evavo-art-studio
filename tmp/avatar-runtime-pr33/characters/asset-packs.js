// Validation-only dependency fixture for exact Council production-status tests.
export const EVAVO_AVATAR_ASSET_PACKS = Object.freeze([
  Object.freeze({ characterId: "eva-female" }),
  Object.freeze({ characterId: "top-hat-man" }),
]);
