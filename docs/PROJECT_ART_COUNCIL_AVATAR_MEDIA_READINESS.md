# Council avatar media readiness

`compileCouncilAvatarMediaReadiness()` is the fail-closed read/compute surface for the current four-seat Council media state. It does not replace the production programme, identity bootstrap, provider transactions, creative review, Runtime publication or website activation.

## Current truth

- `top-hat-man`: reviewed identity exists; three body poses are admitted; six authored performance poses remain missing. The governed Top Hat provider campaign exists, but repository state does not establish the required adapter selection, exact adapter SHA or six slot-specific time-bounded provider authorizations.
- `eva-female`: reviewed identity exists; the ten-master dense bootstrap and full authored animation family remain incomplete. Temporary provenance fallback ordinals are not final release media, and no synthetic body transform may complete the release.
- `council-critic`: canonical four-set, three-view, twelve-job identity bootstrap exists, but no approved identity master or governed character-identity provider execution runtime exists yet.
- `council-open-reviewer`: canonical four-set, three-view, twelve-job identity bootstrap exists, but no approved identity master or governed character-identity provider execution runtime exists yet.

No Council character currently satisfies the complete 749-image authored-animation production gate. Generation never equals approval, and neither Runtime nor website activation is allowed from this readiness result.

## Agent and MCP use

The existing Council production MCP now exposes `evavo_art_council_avatar_media_readiness`. It returns the deterministic stage, blockers, command surfaces and required next evidence for all four characters while retaining zero provider, approval, mutation, publication, deployment or activation authority.

Use `council-avatar-worker-stack` to validate the exact local six-repository estate before relying on readiness state for production planning. Provider execution must still pass its own provider-selection, admission, authorization and receipt gates.
