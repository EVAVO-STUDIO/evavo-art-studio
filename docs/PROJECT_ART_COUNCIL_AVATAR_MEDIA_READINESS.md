# Council avatar media readiness

`compileCouncilAvatarMediaReadiness()` is the fail-closed read/compute surface for the current four-seat Council media state. It does not replace the production programme, identity bootstrap, provider transactions, mastering, creative review, Runtime publication or website activation.

## Current truth

- `top-hat-man`: reviewed identity exists; three body poses are admitted; six authored performance poses remain missing. The governed Top Hat provider campaign exists, but repository state does not establish the required adapter selection, exact adapter SHA or six slot-specific time-bounded provider authorizations.
- `eva-female`: reviewed identity exists and the v2 ten-master production **planning** surface now exists. All ten final dense masters must be new deterministic masters; current `4/5/6` assets remain rollback-only fallback and require remastering for final release. No governed bulk mastering/upload execution evidence is established, no ten-master final set exists, ten-edge continuity remains incomplete, and the full 749-image authored animation family is not released.
- `council-critic`: canonical four-set, three-view, twelve-job identity bootstrap and a governed candidate-only character-identity provider execution surface exist. No real provider selection, admission, time-bounded authorization, execution receipt or approved identity master is established by repository state.
- `council-open-reviewer`: canonical four-set, three-view, twelve-job identity bootstrap and the same governed candidate-only execution surface exist. No real provider selection, admission, time-bounded authorization, execution receipt or approved identity master is established by repository state.

Three characters have a provider execution surface available: Top Hat Man, Council Critic and Council Open Reviewer. EVA instead has a ten-master planning surface with mastering execution still unavailable/unestablished. Zero characters have established real provider execution evidence, and zero Council characters satisfy the complete 749-image authored-animation production gate. Generation or planning never equals approval, and neither Runtime nor website activation is allowed from this readiness result.

## EVA ten-master planning boundary

EVA uses:

- `scripts/project-art/eva-dense-motion-ten-master-program.mjs` for the superseding v2 ten-master contract;
- `scripts/compile-project-art-eva-dense-motion-ten-master.mjs` for create-only deterministic plan compilation;
- `config/eva-dense-motion-workstation-task-v1.json` for the read-only worker validation route.

The existing workstation source preflight intentionally remains seven-frame because ordinals `4/5/6` are currently live rollback frames. The v2 final production contract separately requires ten new masters and marks `4/5/6` as fallback remasters. The workstation validates and reports that planning truth but does not master frames, upload to Cloudinary, approve art, publish Runtime assets or activate the website.

No partial or mixed old/new ten-master family may be promoted. Final activation requires ten unique new immutable master identities, per-frame assurance/mastering/approval evidence, all ten continuity edges including `10 → 1`, regenerated sequence/release evidence and browser verification.

## Character-identity provider boundary

The original Critic and Open Reviewer identities use:

- `scripts/compile-project-art-character-identity-provider-runtime.mjs` for explicit provider admission, one-shot authorization and exact runtime-adapter compilation;
- `scripts/run-project-art-character-identity-provider.mjs` for the actual durable provider transaction;
- exactly one provider/model allow-list entry per admitted job;
- no provider fallback;
- at most one provider call and one runtime attempt per authorization within the authoritative durable runtime root;
- authorization windows no longer than 24 hours;
- candidate-only intermediate artifacts and immutable evidence;
- no candidate, identity, animation, publication or activation approval by generation.

The durable runtime root is part of the operational one-shot boundary: production automation must use the authoritative persistent runtime root rather than creating fresh roots to replay an authorization. The runtime rejects replay once the authorization reservation exists in that root.

Each four-candidate-set campaign is continuity-bound. `full-body-right` must be generated first as the set anchor. `full-body-left` and `neutral-bust` may run only from the exact successful, still-unapproved `full-body-right` candidate artifact from that same set. The runtime re-verifies the immutable artifact before dependent execution and rejects cross-set anchor use.

## Agent and MCP use

The Council production MCP exposes `evavo_art_council_avatar_media_readiness`. It returns deterministic stages, blockers, planning/execution surfaces and required next evidence for all four characters while retaining zero approval, mutation, publication, deployment or activation authority.

Use `council-avatar-worker-stack` to validate the exact local estate before relying on readiness state for production planning. A provider or mastering planning surface being available is not evidence that media bytes were generated, mastered or approved.
