# Pixel Font Studio implementation boundary

The deterministic compiler owns final runtime bytes. Optional provider output is reference-only. Compilation, verification, game activation, native admission, creative approval, repository publication, and force-push authority remain separate operations.
