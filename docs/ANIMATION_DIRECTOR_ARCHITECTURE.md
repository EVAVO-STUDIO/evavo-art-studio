# EVAVO Animation Director

This document is intentionally reserved for the executable Animation Director vertical slice. It must stay aligned with the implementation and tests; architecture-only claims are not sufficient for production readiness.

## Status

Implementation work is in progress on `main`. The first production proof is a deterministic walk-cycle planning vertical slice that binds motion semantics, frame roles, timing, continuity dependencies and provider-neutral rendering requirements before any image generation occurs.
